Go to setup.py, change version string in version number and download url and save
push changes
go to github release page, click draft new release, add version string, then publish
Go to project directory, and build distribution : python setup.py sdist bdist_wheel
Upload to twine : twine upload dist/* --user=**** --pass=****